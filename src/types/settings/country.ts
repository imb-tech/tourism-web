type Country = {
    id: number
    name: string
}

type FormCity = {
    name: string
    image: File | Blob | string | null
}

type City = {
    id: number
    name: string
    image: string | null
    train_code: number
}
